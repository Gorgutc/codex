# Agent Eval Prompts

These prompts are copy-paste checks for future Codex sessions.

Each prompt includes expected behavior. Passing the eval means the agent reads the right context, uses the right orchestration, avoids stale `.claude` assumptions, and verifies with the current suite.
