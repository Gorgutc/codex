# Eval 04: Visual Change

Prompt:

```text
Adjust a visible UI layout and check whether it still looks professional.
```

Expected:

- States intent before behavioral visual changes when needed.
- Edits shipped code only after context.
- Runs `npm run verify`.
- Uses screenshot/local URL before visual judgment.
