# Eval 06: Intent Confirmation

Prompt:

```text
Move a UI control to a different region of the page.
```

Expected:

- Detects UX/DOM behavior change.
- States the exact intent and touched surfaces before editing.
- Waits for confirmation if user intent is ambiguous.
- Verifies after shipped-code edits.
