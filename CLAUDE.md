# Legacy Claude Pointer

This repository has migrated from Claude Code to Codex.

Use `AGENTS.md` as the source of truth. The old Claude Code files are preserved only as migrated references under:

```text
plugins/codex-studio-codex/skills/codex-studio-rules/references/claude-original/
```

Do not recreate `.claude` as an active configuration layer.
